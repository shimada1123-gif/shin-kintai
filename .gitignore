node_modules/
dist/
.output/
.nitro/
.wrangler/
.vite/

# 環境変数（実値はコミットしない）
.env
.env.local
.env.*.local
!.env.example

# OS / editor
.DS_Store
*.log

# Workers のローカル env（service_role / QR_DISPLAY_SECRET を含む）
# サフィックス付きの一時ファイル（.qrtmp 等）も一括で除外する。
.dev.vars
.dev.vars.*

# 否定ルールは除外パターンより後ろに置く（git は最後にマッチした行が勝つ）
!.dev.vars.example
