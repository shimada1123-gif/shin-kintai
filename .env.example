# Supabase (Vite 規約: クライアントに露出するため VITE_ 接頭辞が必須)
# 値は各自の環境で設定すること。実値をコミットしない。
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# service_role キーは .env に置かないこと（VITE_ でなくてもクライアント側の
# import.meta.env には載らないが、混同を避けるため .dev.vars に分離している）
